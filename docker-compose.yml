version: "3"
services:
  mysql:
    environment:
      MYSQL_ROOT_PASSWORD: root
    image: mysql:5.5
    ports:
      - 3306:3306
    restart: always
    volumes:
      - ~/Sites/sumocoders/docker/mysql55:/var/lib/mysql
